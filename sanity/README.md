# Sanity Studio

This is the standalone Sanity Studio configuration for the project.

To run the studio locally:

```bash
cd sanity
npm install
npm run dev
```

Remember to add your `projectId` and `dataset` to `sanity.cli.ts` and `sanity.config.ts`.
